---
title: "Cancel Execution"
api: "POST https://api.defer.run/public/v1/executions/{executionId}/cancel"
---

<RequestExample>

```bash cURL
curl \
  -X POST \
  -u :<access-token> \
  -d '{ "force": true }' \
  https://api.defer.run/public/v1/executions/0ujtsYcgvSTl8PAuAdqWYSMnLOv/cancel
```

</RequestExample>

<ResponseExample>

```json 2xx
{
  "id": "0ujtsYcgvSTl8PAuAdqWYSMnLOv"
}
```

```json 4xx
{
  "error": "bad request",
  "message": "invalid execution id"
}
```

</ResponseExample>

## Endpoint

POST https://api.defer.run/public/v1/executions/{execution-id}/cancel

## Request

### Path Parameters

<ParamField path="execution-id" type="string" required>
  The execution ID.
</ParamField>

### Body Parameters

<ParamField path="force" type="boolean">
  Force the cancellation even if the execution is in running state.
</ParamField>

## Response

### Body

<ResponseField name="id" type="string" required>
  The ID of the execution.
</ResponseField>
