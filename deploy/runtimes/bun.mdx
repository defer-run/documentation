---
title: "Bun"
---

<Info>
  To use Defer with **Bun**, you need the
  [@defer/client](/get-started/quickstart/nextjs#setup) version to be `>=1.11.0`
</Info>

**Bun** background jobs are deployed on the Defer platform using your
`package.json` file.

# Supported Versions

<Note>Only versions `1.x` are supported.</Note>

To use a specific version, or any constraint, add it in the `engines` field of
your `package.json` as follows:

```json
{
  ...
  "engines": {
    "bun": "1.x"
  }
  ...
}
```

# Dependencies

<Note>
  The **Bun** package manager is the only one supported on the **Bun** runtime
  on the Defer platform.
</Note>

Here aren the details of the package manager and its corresponding installation
command ran upon deploy.

| Lockfile    | Package Manager | Command                         |
| ----------- | --------------- | ------------------------------- |
| `bun.lockb` | **Bun**         | `bun install --frozen-lockfile` |

<Info>
  Dependencies are installed with the **exact versions** found in the lockfile
</Info>
